
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Shared.Response
{
    public class BaseResponseModel
   {
     /*  private readonly IBaseRepository<Book> repository;

        public BaseResponseModel(IBaseRepository<Book> repository)
       {
            this.repository = repository;
        }
        public async Task<Book> GetOne(Guid Id)
        {
            var currentInformation = repository.FindAsync(x => x.Id == Id);
            if (currentInformation.Result != null)
            {
                return currentInformation.Result;
            }
            else
            {
                throw new Exception("Id boş olamaz");
            }
        }*/
    }
}
